import 'package:flutter/material.dart';

class TrainerLoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trainer Login'),
      ),
      body: Center(
        child: Text('Trainer Login Screen'),
      ),
    );
  }
}
